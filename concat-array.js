//For concating array
const first = [1, 2];
const second = [3, 4];

const combine = first.concat(second);// Sysntax for concatting two arrays using dot
console.log(combine); // => [1,2,3,4]
console.log(first);
console.log(second);
// why this statement ? New array is created combine . 
